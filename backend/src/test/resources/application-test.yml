spring:
  datasource:
    url: jdbc:h2:mem:qinggan;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
qinggan:
  family:
    join-token: test-family-join-token
    device-token-signing-secret: 0123456789abcdef0123456789abcdef
